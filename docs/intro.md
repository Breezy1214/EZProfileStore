# EZProfileStore

EZProfileStore wraps ProfileStore with a small server API for profile lifecycle and replicated mutations, plus a client API for reading a Fusion-backed local mirror.

Start with the README for installation and examples, then use the API pages for method-level details generated from the Moonwave comments in `src/`.
